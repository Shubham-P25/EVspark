import { MiniMap } from './mini-map'

export function RoleMock({ role }: { role: string }) { if (role === 'Drivers') return <div className="role-mock"><MiniMap /><div className="nearest">Nearest: 0.8 km — <b>3 chargers free</b></div></div>; if (role === 'Operators') return <div className="role-mock operator-mock"><div><small>REVENUE</small><b>₹24,300</b></div><div><small>SESSIONS</small><b>48</b></div><div><small>UPTIME</small><b>99.2%</b></div><div className="tiny-bars">{[40,70,55,90,65,80].map((h,i)=><i key={i} style={{height:`${h}%`}} />)}</div></div>; return <div className="role-mock admin-mock">{['Tata Power · Active', 'Ather Grid · Flagged', 'HPCL EV · Pending'].map((x,i)=><div key={x}><span>{x}</span><b className={i===0?'active':i===1?'flagged':'pending'}>{i===0?'Active':i===1?'Flagged':'Pending'}</b></div>)}</div> }