import { Card } from "@/components/ui/Card";
import { Stat } from "@/components/ui/Stat";
import { TopBar } from "@/components/layout/TopBar";
import { MOCK_NETWORK_STATS, MOCK_STATIONS } from "@/lib/mockData";

export default function AdminAnalyticsPage() { return <><TopBar eyebrow="Admin / Network analytics" title="Mumbai network health" /><main className="space-y-8 p-5 sm:p-8"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3"><Stat label="Total stations" value={MOCK_NETWORK_STATS.totalStations} /><Stat label="Active stations" value={MOCK_NETWORK_STATS.activeStations} /><Stat label="Total sessions" value={MOCK_NETWORK_STATS.totalSessions} /><Stat label="Network revenue" value={MOCK_NETWORK_STATS.networkRevenue} prefix="₹" /><Stat label="Average utilization" value={MOCK_NETWORK_STATS.avgUtilization} suffix="%" /><Stat label="Forecast accuracy" value={MOCK_NETWORK_STATS.modelAccuracy} suffix="%" /></div><Card className="overflow-hidden p-5"><div className="flex items-center justify-between"><div><h2 className="font-medium">Network station map</h2><p className="mt-1 text-sm text-muted">Bird&apos;s-eye view of {MOCK_STATIONS.length} simulated stations across Mumbai.</p></div><span className="font-mono text-xs text-primary">LIVE</span></div><div className="ev-grid relative mt-5 grid min-h-[360px] grid-cols-10 gap-2 overflow-hidden border border-border bg-bg p-5">{MOCK_STATIONS.map((station) => <span key={station.id} title={`${station.name}: ${station.status}`} className={`h-2.5 w-2.5 rounded-full ${station.status === "red" ? "bg-red" : station.status === "yellow" ? "bg-yellow" : "bg-green"}`} style={{ marginTop: `${(station.lat - 18.9) * 90}%`, marginLeft: `${(station.lng - 72.8) * 30}%` }} />)}</div></Card></main></>; }